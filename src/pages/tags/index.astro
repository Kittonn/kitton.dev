---
import { getUniqueTags } from "../../utils/content";

const tags = await getUniqueTags();
---

<h1>tags</h1>
<ul>
  {
    tags.map((tag: string) => (
      <li>
        <a href={`/tags/${tag}`}>{tag}</a>
      </li>
    ))
  }
</ul>
